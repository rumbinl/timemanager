#pragma once

#include <TM_Core.hpp>

struct TM_ViewSetting { SkColor backgroundColor, borderColor, textColor; SkScalar borderThickness, fontSize, paddingX, paddingY; bool icon=false; SkScalar cornerRadius=0.0f;};